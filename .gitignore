# PyCharm
.idea/

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
env/

# dbt
02_dbt/target/
02_dbt/dbt_packages/
02_dbt/logs/
# machine-local anonymous id, regenerated by dbt
02_dbt/.user.yml
# regenerated by `dbt deps`; packages.yml already pins exact versions
02_dbt/package-lock.yml

# Environment
.env
*.env

# Terraform — local state and personal config
terraform.tfvars
tfplan
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# Claude Code — machine-local settings (may contain personal permissions or secrets)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db
